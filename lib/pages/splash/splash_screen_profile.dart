

import 'package:flutter/material.dart';

class SplashScreenClientProfile extends StatelessWidget {
  const SplashScreenClientProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hola"),
      ),
    );
  }
}